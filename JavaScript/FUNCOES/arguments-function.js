/*
    FUNCTION ARGUMENTOS E PARÂMETROS
             ARGUMENT AND PARAMETERS
    
    Estrutura:

    function nome_da_funcao(parâmetros, ...){
        console.log(parâmetros + ...)
    }

    nome_da_funcao(argumentos EX: 2,3) // vai fazer a soma dos dois números

    Posso colocar quantos argumentos eu quiser
    Só posso chamar(invocar) a function após ela existir
    */

    function sum(number1, number2){
        console.log(number1 + number2)
    }

    sum(5,9)